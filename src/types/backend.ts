export interface User {
  id: string;
  email: string;
  password: string;
  username: string;
  phone: string;
  role: string;
  createdAt: string;
  updatedAt: string;
  isActive: boolean;
  accessToken: string;
}
